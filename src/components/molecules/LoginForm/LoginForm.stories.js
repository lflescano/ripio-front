import React from "react";

import LoginForm from '.';

export default {
    title: 'Molecules/LoginForm'
}

export const Default = () => {
    return <LoginForm/>
}

export const Mobile = () => {

    return <LoginForm/>
}